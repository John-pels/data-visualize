/*=============================================
=         Routes declaration and initialization       =
=============================================*/
/**
 *
 * Here, we use enum to store our routes for scalbility, debugging and maintainance purposes
 *
 */

export enum API_ROUTES {
  PORTS = "/ports",
  MARKET_RATES = "/rates",
}
